# Handoff — пилот Jev в selena-OS

Дата: 2026-09-23
Репозиторий: `Selena-AI-projects/selena-OS`
Кто писал: Claude Code (сессия `session_01BLKvMZ2oEMwvgb2fW4J445`), правки и решения — по ходу диалога с владельцем проекта.

Этот файл — единая точка входа: что за задача, что сделано, что не сделано, что нужно решить дальше. Подробности — в `JEV-PILOT.md`.

---

## Задача одной фразой

Проверить (не внедрить), может ли модель Jev (TypeSafe AI) улучшить одно узкое место в Elmo — отличать «наш бренд действительно упомянут» от «слово случайно совпало с чем-то другим» (например, «Elmo» — продукт vs «Elmo» — персонаж Sesame Street). Сейчас это делается тупым поиском подстроки, без понимания смысла.

## Где что лежит

| Ветка | PR | Статус | Что в ней |
|---|---|---|---|
| `claude/selena-typesafe-integration-y98vbj` | [#58](https://github.com/Selena-AI-projects/selena-OS/pull/58) | открыт, не смёржен | базовая реализация пилота: код, тесты, первый реальный тест (7 кейсов) |
| `claude/jev-collision-benchmark` | [#59](https://github.com/Selena-AI-projects/selena-OS/pull/59), stacked на #58 | открыт, не смёржен | 26 collision-кейсов, исправленный вопрос к Jev, код-ревью и правки, реальный тест на 32 кейсах, вердикт |
| `claude/jev-shadow-test`, stacked на #59 | **PR не создан** | закоммичено локально (`8e74a282`), **не запушено** | инструмент для shadow-test на реальных (не клиентских) текстах — написан, не запускался |

Все три ветки — в изолированной папке `apps/worker/src/pilots/jev-mentions/`, не подключены к основному пайплайну. Ключ `JEV_PILOT_ENABLED` нигде не включён.

## Что сделано

### Код и инфраструктура
- Изолированный модуль пилота: классификация упоминаний через Jev (Noul-примитив), с выключателем по умолчанию, автоматическим откатом на старую эвристику при любой ошибке.
- Официальный skill/плагин TypeSafe установлен **только на уровень проекта** (не глобально).
- Выбран и явно закреплён canonical baseline (`analyzeMentions` в `process-prompt.ts`, а не вторая, разошедшаяся копия в `report-worker.ts`) — доказано тестом, который реально **исполняет** живой код оригинала, а не сравнивает текст.
- Отдельный код-ревью (`/code-review`, не тот же проход, что писал код): 6 находок, 3 реальных бага исправлены (падение вместо честного `insufficient_data` на пустом `brand`; дублирование логики dry-run; тест сравнивал текст исходника вместо поведения — переписан).

### Метрика и семантика
- Пересмотрено, что вообще значит «упоминание» в продукте: только присутствие + правильная атрибуция сущности, **не тональность** — в схеме БД нет поля sentiment, я ошибочно ранее считал негативное/отрицающее упоминание «ошибкой», это было неверно и исправлено.
- Переформулирован вопрос к Jev: раньше он смешивал «это та сущность?» с «текст её не отрицает?» — теперь строго только атрибуция.

### Проверка применимости
- Найдено и зафиксировано: **не подтверждено**, что код в `selena-OS` вообще работает в реальном продакшене. По документам самого репозитория (`docs/execution/SNAPSHOT.md`), реальный трафик обслуживает **другой** репозиторий — `parkourcafe/selena-ai-visibility`, независимый форк с разошедшимся кодом с версии миграции `0021`. Доступа к нему у этой сессии нет и не запрашивался.

### Реальные тесты (оба — на вашем Mac, из моей песочницы `api.typesafe.ai` заблокирован сетевой политикой)
1. **Первый прогон, 7 кейсов**: $0.000120, модель `jev-1.13.0`, 8/10 совпадений с эталоном. Один явный баг в формулировке вопроса (см. выше), один флагманский кейс («Elmo из Sesame Street») вышел «не уверен» (0.57).
2. **Второй прогон, 32 кейса** (после исправления вопроса + 26 новых collision-кейсов на реальных омонимах — компания ELMO, деловой акроним, «profound» как прилагательное и т.д.): $0.000951. Категория `negation` — 2/2 (баг подтверждён и исправлен). Категория `collision` (ради которой всё затевалось) — **50/52 правильно, 0 ошибок, 2 неопределённых**. Флагманский кейс теперь — уверенный правильный ответ (0.18).
3. **Прямое сравнение со старой эвристикой** на тех же 32 кейсах (посчитано локально, без API): эвristика — 34/52 (65%) на collision-кейсах, часто уверенно неправильно. Jev — 96%, ни разу уверенно неправильно.

### Итоговый вердикт (см. `JEV-PILOT.md`)
**A. PROMISING** — разрыв большой и однонаправленный, чтобы списать на случайность, но `BENCHMARK_VERIFIED` не заявлен (26 кейсов размечены мной, не независимой командой на реальных данных). `NO_BENEFIT` явно отклонён.

### CI по PR #58 и #59
Найдено: обязательные проверки в branch protection физически не могут пройти — 18 сентября в репозитории **намеренно отключили автозапуск GitHub Actions** (коммит `chore: disable automatic workflow runs`, база обоих PR). Все workflow теперь `workflow_dispatch`-only. Это не связано с содержимым наших PR — несостыковка политики репозитория с настройками защиты веток.

### Дисциплина по безопасности и разрешениям
- Пуш — **только после явного «да»** от вас, каждый раз отдельно (5+ раз за сессию).
- API-ключ — никогда не печатался в файлы/код/коммиты; передавался через `--env-file` во временный файл вне репозитория, удалялся сразу после использования; вам дважды предлагалось ротировать ключ (он проходил через чат).
- Ни одного реального API-вызова без вашего явного согласия на объём данных и бюджет.

## Что НЕ сделано

- **`BENCHMARK_VERIFIED` не достигнут.** 26 collision-кейсов — сильный сигнал, но не замена реальной размеченной выборке.
- **Shadow-test не запущен.** Инструмент (`run-shadow-test.ts`) написан и проверен на отказоустойчивость, но требует от вас реальных (не клиентских) текстов ответов AI-движков — их ещё нет.
- **Реальная 150-примерная held-out выборка из продакшн-данных — заблокирована.** Нет доступа к БД из песочницы, и неясно, тот ли это вообще репозиторий, где крутится продакшн.
- **CI по PR #58/#59 не решён.** Нужно ваше решение: вручную запускать workflow для этих PR через `workflow_dispatch`, или менять branch protection.
- **PR #58 и #59 не смёржены.**
- **Ветка `claude/jev-shadow-test` не запушена** — нет PR.
- **`INDEPENDENT_REVIEW` по-прежнему `NOT_PERFORMED`** — код-ревью было сделано мной же через тулинг, не отдельным человеком/ревьюером.
- **`JEV_PILOT_ENABLED` нигде не включён** — сознательно, до появления реальных данных для полноценного бенчмарка.
- Продакшн, БД, `selena-ai-visibility` — не трогались вообще.

## Что нужно от вас, чтобы продолжить

1. **Для shadow-test**: сгенерировать 10-15 реальных ответов ChatGPT/Perplexity на свои же тестовые вопросы (без клиентских данных), положить в `shadow-input.json` (по примеру `shadow-input.example.json`), прогнать `run-shadow-test.ts` на вашем Mac.
2. **Для CI**: решить — вручную дёргать workflow для PR #58/#59, или чинить branch protection.
3. **Для PR**: сказать, когда мёржить #58/#59, и нужен ли PR для ветки `claude/jev-shadow-test`.
4. **Для полного бенчмарка**: решить, стоит ли добывать доступ к реальным данным (и в каком репозитории — `selena-OS` или `selena-ai-visibility`), учитывая находку про расхождение кода.

Всё остальное — код, тесты, отчёты, вердикт — готово и лежит в перечисленных ветках.
