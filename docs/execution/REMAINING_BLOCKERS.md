# Оставшиеся блокеры

Только внешние зависимости — то, что физически не может сделать исполнитель.
TODO в коде блокером не считается и сюда не попадает.

Формат каждого пункта: одно точное действие, безопасный способ, что уже сделано
рядом с ним и что продолжится автоматически после.

Состояние на 2026-09-04. Три блокера предыдущей редакции сняты: доступ Railway к
репозиторию выдан и контур развёрнут, DNS для `studio` создан и распространился,
мост доведён до сквозного прогона на живом контуре.

Пункт 2 переписан: прежняя формулировка называла неверную причину и неверное
действие. Разбор — внутри пункта.

---

## 1. Сертификат studio.selenasystems.com

**Действие.** В панели DNS-провайдера проверить записи **на имени `studio`** —
нет ли рядом с CNAME чего-то ещё: `A`, `AAAA`, `TXT`, собственной записи `CAA`,
или проксирования (в Cloudflare — оранжевое облако вместо серого).

**Почему.** DNS в порядке: Railway видит `CNAME studio → iu5dnw7q.up.railway.app`
со статусом `PROPAGATED`. Но сертификат больше суток стоит в
`VALIDATING_OWNERSHIP`, и Railway не сообщает причины — `errorMessage` пуст.

**Что уже исключено.** `os.selenasystems.com` живёт в той же зоне, на том же
сервисе, и его сертификат **валиден**. Значит запрета на уровне зоны нет: Let's
Encrypt для `selenasystems.com` уже выдавал сертификат. Причина — в чём-то, что
относится именно к имени `studio`.

**Что пробовали.** Домен сняли и добавили обратно через агента Railway, чтобы
перезапустить выдачу. Результата нет: идентификатор домена остался прежним, цель
CNAME та же, статус сертификата не изменился — то есть настоящего пересоздания не
произошло и новый заказ не стартовал. Прикладной сервис при этом не пострадал:
после раскатки изменений пул к базе поднялся штатно.

**Что ещё исключено.** Подложить корневой сертификат Supabase и включить проверку
обратно (это сняло бы блокер 2, а не этот) — отвергнуто: сертификат нашёлся только
в двух посторонних репозиториях, а доверенный корень из непроверенного источника
хуже нынешнего состояния. Прямой доступ к DNS из среды исполнения закрыт сетевой
политикой, поэтому увидеть записи на имени `studio` можно только из панели
провайдера.

**Продолжится после.** Проверка входа, выхода, обновления сессии и callback на
новом адресе; `os.` при этом не трогается и продолжает работать.

---

## 2. Корневой сертификат Supabase для Aether

**Действие.** Supabase → Project Settings → **Database** → SSL Configuration →
скачать сертификат (`prod-ca-2021.crt`). Открыть его текстовым редактором,
скопировать содержимое целиком — вместе со строками `-----BEGIN CERTIFICATE-----`
и `-----END CERTIFICATE-----` — и вставить в новую переменную
`AETHER_DB_SSL_ROOT_CERT_PEM` у Railway-сервиса Aether. Дождаться старта и
проверить, что в логе нет `DB pool init failed`. **После этого** удалить
`AETHER_DB_SSL_INSECURE`.

Строку подключения менять не нужно и не следует.

**Предыдущая редакция этого пункта была неверна.** Она требовала перевести
`DATABASE_URL` на пулер и обещала, что это снимет блокер. Перевод не помог бы:
пулер уже используется, а причина отказа в другом.

**Чем это установлено.**

1. У сервиса Aether в Railway выключен исходящий IPv6 (`ipv6EgressEnabled:
   false`), а прямой хост `db.<ref>.supabase.co` отдаёт только AAAA-запись —
   A-записи у него нет. То есть прямой хост из этого сервиса недостижим в
   принципе, и работающее приложение подключено не через него.
2. В `pg_stat_activity` живой базы соединения приложения приходят с того же
   адреса, что и соединения служебной роли `pgbouncer`, — то есть с адреса
   самого пулера, а не с адреса Railway.
3. Отказ в логе деплоя `2de6416a` дословно: `DB pool init failed:
   SSLCertVerificationError: [SSL: CERTIFICATE_VERIFY_FAILED] certificate verify
   failed: self-signed certificate in certificate chain`. Это не «неизвестный
   издатель» и не «имя не совпало», а самоподписанный корень, которого нет в
   системном хранилище.
4. Документация Supabase подтверждает: для `verify-full` нужен их корневой
   сертификат, и в примере он указан **вместе с хостом пулера**. Пулер даёт
   IPv4, а не доверенную цепочку.

**Что сделано, чтобы это стоило одной вставки.** Рантайм теперь принимает корень
не только файлом в образе, но и значением переменной — включая случай, когда
панель хостинга отдаёт многострочное значение с экранированными переносами.
Заданный, но нечитаемый корень не даёт пулу встать: молчаливого возврата к
системному хранилищу нет, иначе настройка ради проверки её бы и отключила
(коммит `dd34c6b` в Aether, тесты `test_database_tls.py`).

**Почему не сделано исполнителем.** Сертификат нужно взять по каналу, который
сам подтверждён. Все адреса Supabase закрыты сетевой политикой среды — шлюз
отвечает `403` на CONNECT к `supabase.com`, `api.supabase.com` и к хосту
проекта. Прочитать файл сертификата через SQL нельзя: `pg_read_file` для роли
проекта запрещён. Копия из постороннего репозитория отвергнута по той же
причине, что и раньше: доверенный корень из непроверенного источника хуже
нынешнего состояния.

**Ещё одна ловушка, из-за которой это легко сделать неправильно.** В дашборде
четыре строки подключения, и порт `6543` есть у двух разных: у общего пулера
(`aws-…pooler.supabase.com`, годится) и у выделенного PgBouncer
(`db.<ref>.supabase.co`, IPv6-only — сервис с ней просто не поднимется).
Отличать надо по хосту, а не по порту; таблица добавлена в `docs/SETUP.md`
Aether.

---

## 3. Перевыпуск секрета моста

**Действие.** Заменить `SELENA_AETHER_BRIDGE_SECRET` у сервиса `receiver` и
`CONTROL_ROOM_BRIDGE_SECRET` у Aether. Обе стороны принимают список через
запятую: поставить новый первым, старый вторым, дождаться успешной доставки,
затем убрать старый — простоя не будет.

**Почему.** Владелец попросил выполнить перенос самостоятельно, а прочитать уже
установленное значение через API нельзя — оно скрыто. Поэтому значение было
сгенерировано в сессии и прошло через её протокол. Это секрет подписи, не
доступа к данным: им можно подделать событие для staging-приёмника, но нельзя
ничего прочитать.

---

## 4. Ключи подписи для сервисов gateway и scanner

**Действие.** Создать ключевую пару подписи релизов и учётные данные хранилища,
задать их переменными сервисов и создать сервисы `gateway`, `scanner`, `worker`
в проекте `selena-os-staging` (имя сервиса выбирает стадию сборки).

**Почему это не сделано исполнителем.** Приватный ключ подписи релизов
существует ровно затем, чтобы им владел один компонент. Сгенерировать его в
сессии — значит записать его в протокол сессии, что противоречит смыслу ключа.

**На доказательства не влияет.** Gate 7 и Gate 8 закрыты тестами и pgTAP на
настоящей базе; отсутствие этих сервисов не оставляет ни одного непроверенного
утверждения ТЗ.

---

## 5. Проверка восьми разделов Control Room в браузере

**Действие.** Открыть `https://web-production-6cb6b.up.railway.app` и пройти
восемь разделов.

**Почему не сделано исполнителем.** Сетевая политика этой среды не пропускает
`*.up.railway.app`: запрос отклоняется на прокси. Разделы проверены живым
запуском приложения на настоящей мигрированной схеме — но не на развёрнутом
контуре и не глазами.
