---
"@workspace/web": patch
"@workspace/worker": patch
---

Measurement runs on the inert noop adapter no longer fail on a missing OpenRouter key; the operator desk explains its gates in plain language and keeps the order queue fresh after a failed start; the registration and password screens redirect without a server-render error.
